# Memory System v1.2.5 - 关键缺陷修复方案

## 🔥 审计发现的致命问题

基于 Crabby 的深度审计，发现 5 个架构级缺陷：

| 问题 | 严重性 | 影响 |
|------|--------|------|
| 1. 并发死锁风险 | 🔴 Critical | 高峰期崩溃 |
| 2. 衰减逻辑失效 | 🔴 Critical | 检索质量退化 |
| 3. SQL 注入风险 | 🟡 High | 数据库被清空 |
| 4. 笛卡尔积陷阱 | 🟡 High | 性能退化 |
| 5. 版本管理缺失 | 🟠 Medium | 升级困难 |

---

## 🎯 修复策略

### 原则
1. **安全优先**：先修 SQL 注入和并发问题
2. **逻辑正确**：修复衰减计算
3. **性能优化**：解决笛卡尔积
4. **可维护性**：引入版本管理

### 不采用的方案
❌ **引入 SQLAlchemy**：
- 理由：增加 10MB+ 依赖，违背"轻量级"原则
- 替代：手写安全的参数化查询 + 连接池

---

## 📋 修复清单

### Phase 1: 并发安全（P0，2h）

#### 1.1 连接池 + 写锁

**问题**：
```python
def _get_connection(self):
    return sqlite3.connect(self.db_path)  # ❌ 每次新建连接
```

**修复**：
```python
import threading
from contextlib import contextmanager

class SQLiteBackend:
    def __init__(self, memory_dir: Path):
        self._conn_lock = threading.RLock()  # 可重入锁
        self._conn = None
        self._write_lock = threading.Lock()  # 写操作锁
    
    @contextmanager
    def _get_connection(self, write=False):
        """上下文管理器，自动加锁"""
        lock = self._write_lock if write else self._conn_lock
        with lock:
            if self._conn is None:
                self._conn = sqlite3.connect(
                    self.db_path,
                    check_same_thread=False,  # 允许多线程
                    timeout=30.0  # 30秒超时
                )
                self._conn.row_factory = sqlite3.Row
                self._conn.execute('PRAGMA journal_mode=WAL')
            yield self._conn
    
    def insert_memory(self, record: Dict):
        with self._get_connection(write=True) as conn:
            cursor = conn.cursor()
            # ... 插入逻辑
            conn.commit()
```

**效果**：
- ✅ 单例连接，避免连接泄漏
- ✅ 写操作互斥，避免死锁
- ✅ 30秒超时，避免永久阻塞

#### 1.2 事务隔离级别

```python
def _init_db(self):
    with self._get_connection(write=True) as conn:
        conn.execute('PRAGMA journal_mode=WAL')
        conn.execute('PRAGMA synchronous=NORMAL')
        conn.execute('PRAGMA busy_timeout=30000')  # 30秒
        conn.execute('PRAGMA cache_size=-64000')   # 64MB 缓存
```

---

### Phase 2: SQL 注入防护（P0，1h）

#### 2.1 参数化查询审计

**问题**：
```python
# ❌ 危险：动态拼接 placeholders
placeholders = ','.join(['?'] * len(entities))
cursor.execute(f'''
    SELECT ... WHERE me.entity IN ({placeholders})
''', entities)
```

**修复**：
```python
# ✅ 安全：使用参数化查询
def search_by_entities(self, entities: List[str]) -> List[Dict]:
    if not entities:
        return []
    
    # 参数化查询，避免注入
    placeholders = ','.join(['?'] * len(entities))
    query = f'''
        SELECT DISTINCT m.*
        FROM memories m
        JOIN memory_entities me ON m.id = me.memory_id
        WHERE me.entity IN ({placeholders})
        AND m.state = 0
        ORDER BY m.final_score DESC
    '''
    
    with self._get_connection() as conn:
        cursor = conn.cursor()
        cursor.execute(query, entities)
        return [dict(row) for row in cursor.fetchall()]
```

#### 2.2 输入验证

```python
def _sanitize_entity(self, entity: str) -> str:
    """清理实体名称，防止注入"""
    # 移除危险字符
    dangerous = [';', '--', '/*', '*/', 'DROP', 'DELETE', 'UPDATE']
    entity = entity.strip()
    for d in dangerous:
        if d in entity.upper():
            raise ValueError(f"非法实体名称: {entity}")
    return entity[:100]  # 限制长度
```

---

### Phase 3: 衰减逻辑重构（P0，3h）

#### 3.1 问题分析

**当前设计**：
```sql
final_score REAL GENERATED ALWAYS AS (score + access_boost) STORED
```

**问题**：
- `final_score` 是静态的，不会随时间自动衰减
- 需要手动运行 `consolidation` 才更新
- 三个月不用，旧记忆依然高分

#### 3.2 解决方案：动态计算

**方案 A：查询时计算（推荐）**

```python
def search_by_score(self, limit: int = 50) -> List[Dict]:
    """检索时动态计算 final_score"""
    query = '''
        SELECT 
            m.*,
            -- 动态计算衰减后的分数
            (m.score * POWER(?, julianday('now') - julianday(m.created))) 
            + m.access_boost AS dynamic_score
        FROM memories m
        WHERE m.state = 0
        ORDER BY dynamic_score DESC
        LIMIT ?
    '''
    
    decay_rate = 0.992  # 每天衰减 0.8%
    
    with self._get_connection() as conn:
        cursor = conn.cursor()
        cursor.execute(query, (decay_rate, limit))
        return [dict(row) for row in cursor.fetchall()]
```

**方案 B：后台定时更新（备选）**

```python
def apply_decay(self):
    """批量应用衰减（每天运行一次）"""
    query = '''
        UPDATE memories
        SET score = score * POWER(?, julianday('now') - julianday(updated)),
            updated = datetime('now')
        WHERE state = 0
    '''
    
    decay_rates = {
        'fact': 0.992,     # 0.8%/天
        'belief': 0.93,    # 7%/天
        'summary': 0.975   # 2.5%/天
    }
    
    with self._get_connection(write=True) as conn:
        for mem_type, rate in decay_rates.items():
            conn.execute(query, (rate,))
        conn.commit()
```

**推荐**：方案 A（查询时计算）
- ✅ 实时准确
- ✅ 不依赖定时任务
- ⚠️ 查询稍慢（但可接受）

---

### Phase 4: 笛卡尔积优化（P1，2h）

#### 4.1 问题分析

**当前查询**：
```sql
SELECT m.*, GROUP_CONCAT(me.entity) AS entities
FROM memories m
LEFT JOIN memory_entities me ON m.id = me.memory_id
GROUP BY m.id
```

**问题**：
- 一个记忆关联 5 个实体 → 中间表 5 行
- 100 条记忆 × 平均 3 实体 = 300 行中间表

#### 4.2 优化方案

**方案 A：子查询（推荐）**

```python
def get_all_memories(self, state: int = 0) -> List[Dict]:
    """优化的全量查询"""
    query = '''
        SELECT 
            m.*,
            (SELECT GROUP_CONCAT(entity) 
             FROM memory_entities 
             WHERE memory_id = m.id) AS entities
        FROM memories m
        WHERE m.state = ?
        ORDER BY m.final_score DESC
    '''
    
    with self._get_connection() as conn:
        cursor = conn.cursor()
        cursor.execute(query, (state,))
        results = []
        for row in cursor.fetchall():
            record = dict(row)
            record['entities'] = record['entities'].split(',') if record['entities'] else []
            results.append(record)
        return results
```

**方案 B：两次查询（大数据量）**

```python
def get_all_memories_optimized(self, state: int = 0) -> List[Dict]:
    """两次查询，避免笛卡尔积"""
    with self._get_connection() as conn:
        cursor = conn.cursor()
        
        # 第一次：查询记忆
        cursor.execute('SELECT * FROM memories WHERE state = ?', (state,))
        memories = [dict(row) for row in cursor.fetchall()]
        
        # 第二次：批量查询实体
        memory_ids = [m['id'] for m in memories]
        placeholders = ','.join(['?'] * len(memory_ids))
        cursor.execute(f'''
            SELECT memory_id, GROUP_CONCAT(entity) AS entities
            FROM memory_entities
            WHERE memory_id IN ({placeholders})
            GROUP BY memory_id
        ''', memory_ids)
        
        # 合并结果
        entity_map = {row['memory_id']: row['entities'].split(',') 
                      for row in cursor.fetchall()}
        
        for m in memories:
            m['entities'] = entity_map.get(m['id'], [])
        
        return memories
```

---

### Phase 5: 版本管理（P2，1h）

#### 5.1 Schema 版本表

```sql
CREATE TABLE IF NOT EXISTS schema_version (
    version INTEGER PRIMARY KEY,
    applied_at TEXT NOT NULL,
    description TEXT
);

INSERT INTO schema_version (version, applied_at, description)
VALUES (1, datetime('now'), 'Initial schema');
```

#### 5.2 迁移框架

```python
class Migration:
    """数据库迁移管理"""
    
    MIGRATIONS = {
        1: {
            'description': 'Initial schema',
            'up': SCHEMA_SQL,
            'down': 'DROP TABLE IF EXISTS memories; ...'
        },
        2: {
            'description': 'Add dynamic_score support',
            'up': '''
                ALTER TABLE memories ADD COLUMN decay_applied_at TEXT;
            ''',
            'down': '''
                -- SQLite 不支持 DROP COLUMN，需要重建表
            '''
        }
    }
    
    def get_current_version(self, conn) -> int:
        """获取当前版本"""
        try:
            cursor = conn.cursor()
            cursor.execute('SELECT MAX(version) FROM schema_version')
            result = cursor.fetchone()
            return result[0] if result[0] else 0
        except sqlite3.OperationalError:
            return 0
    
    def migrate(self, target_version: int = None):
        """执行迁移"""
        with self._get_connection(write=True) as conn:
            current = self.get_current_version(conn)
            target = target_version or max(self.MIGRATIONS.keys())
            
            for version in range(current + 1, target + 1):
                migration = self.MIGRATIONS[version]
                print(f"应用迁移 v{version}: {migration['description']}")
                conn.executescript(migration['up'])
                conn.execute('''
                    INSERT INTO schema_version (version, applied_at, description)
                    VALUES (?, datetime('now'), ?)
                ''', (version, migration['description']))
                conn.commit()
```

---

## 📊 修复优先级

| Phase | 任务 | 时间 | 优先级 | 依赖 |
|-------|------|------|--------|------|
| 1 | 并发安全 | 2h | P0 | - |
| 2 | SQL 注入防护 | 1h | P0 | - |
| 3 | 衰减逻辑重构 | 3h | P0 | Phase 1 |
| 4 | 笛卡尔积优化 | 2h | P1 | Phase 1 |
| 5 | 版本管理 | 1h | P2 | - |

**总计**：9 小时

---

## 🧪 测试计划

### 1. 并发测试

```python
import threading

def test_concurrent_writes():
    """测试并发写入"""
    backend = SQLiteBackend(memory_dir)
    
    def write_memory(i):
        backend.insert_memory({
            'id': f'test_{i}',
            'content': f'Test memory {i}',
            'entities': ['test']
        })
    
    threads = [threading.Thread(target=write_memory, args=(i,)) 
               for i in range(100)]
    
    for t in threads:
        t.start()
    for t in threads:
        t.join()
    
    # 验证：100 条记忆全部写入
    assert len(backend.get_all_memories()) == 100
```

### 2. SQL 注入测试

```python
def test_sql_injection():
    """测试 SQL 注入防护"""
    backend = SQLiteBackend(memory_dir)
    
    # 尝试注入
    malicious_entity = "'; DROP TABLE memories; --"
    
    try:
        backend.search_by_entities([malicious_entity])
        assert False, "应该抛出异常"
    except ValueError:
        pass  # 预期行为
    
    # 验证：数据库未被破坏
    assert backend.get_all_memories() is not None
```

### 3. 衰减测试

```python
def test_decay_logic():
    """测试衰减逻辑"""
    backend = SQLiteBackend(memory_dir)
    
    # 插入旧记忆（90 天前）
    old_memory = {
        'id': 'old_001',
        'content': 'Old memory',
        'score': 1.0,
        'created': (datetime.now() - timedelta(days=90)).isoformat()
    }
    backend.insert_memory(old_memory)
    
    # 插入新记忆
    new_memory = {
        'id': 'new_001',
        'content': 'New memory',
        'score': 1.0,
        'created': datetime.now().isoformat()
    }
    backend.insert_memory(new_memory)
    
    # 检索：新记忆应该排在前面
    results = backend.search_by_score(limit=10)
    assert results[0]['id'] == 'new_001'
```

---

## 🎯 成功指标

| 指标 | 目标 | 验证方法 |
|------|------|---------|
| 并发安全 | 100 线程无死锁 | 并发测试 |
| SQL 注入防护 | 100% 拦截 | 注入测试 |
| 衰减准确性 | 误差 < 5% | 衰减测试 |
| 查询性能 | < 50ms (1000 条) | 性能测试 |
| 版本迁移 | 无数据丢失 | 迁移测试 |

---

## 📝 实施步骤

1. **创建分支**：`git checkout -b fix/v1.2.5-critical-fixes`
2. **逐个修复**：按 Phase 1-5 顺序
3. **单元测试**：每个 Phase 完成后测试
4. **集成测试**：全部完成后压测
5. **文档更新**：更新 SKILL.md 和使用指南
6. **发布**：合并到 main，打 tag v1.2.5

---

## 🙏 致谢

感谢 Crabby 的深度审计，指出了这些致命缺陷。这份报告让我们意识到：

> **性能优化不能以牺牲安全和正确性为代价。**

---

**文档版本**: v1.0  
**创建时间**: 2026-02-14 04:15 UTC  
**作者**: Tkao  
**预计完成**: 2026-02-14 13:00 UTC
