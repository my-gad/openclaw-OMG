# Memory System v1.3.0 开发计划
## 基于评测集调研的系统性升级方案

---

## 📊 调研核心发现

### 四大评测集对比

| 评测集 | 核心考察 | SOTA 分数 | 我们的最大差距 |
|--------|---------|-----------|---------------|
| **LoCoMo** | 长会话一致性 | 93.05% | 证据追踪 + 多模态索引 |
| **LongMemEval** | 百万级扩展性 | 83% | 时序推理 + 事实演变追踪 |
| **HaluMem** | 操作级幻觉检测 | 86.35% | 冲突消解 + 虚假记忆过滤 |
| **PersonaMem v2** | 隐式画像学习 | 55% | 隐式偏好提取 + 归属识别 |

### 当前能力 vs 评测要求

| 能力维度 | 当前状态 | 评测要求 | 优先级 |
|---------|---------|---------|--------|
| 冲突处理 | ❌ 简单覆写 | ✅ 置信度 + 时序消解 | 🔴 P0 |
| 虚假记忆过滤 | ❌ 无过滤 | ✅ FMR > 85% | 🔴 P0 |
| 时序推理 | ⚠️ 基础支持 | ✅ 时间跳跃 + 演变追踪 | 🟡 P1 |
| 隐式画像 | ❌ 仅关键词 | ✅ 行为模式推理 | 🟠 P2 |
| 证据追踪 | ❌ 无溯源 | ✅ 返回来源 ID | 🟡 P1 |
| 百万级索引 | ⚠️ SQLite 未压测 | ✅ 向量-图混合 | 🟡 P1 |

---

## 🎯 三阶段演进路线

### Phase 1: 基础能力补齐（必须）
**目标**: 通过 HaluMem 基本测试，解决幻觉累积问题

**预计工作量**: 120 小时 → **简化为 40-60 小时**

#### 1.1 Memory Operator（记忆操作决策引擎）

**参考**: Mem0 的 ADD/UPDATE/DELETE/NOOP 架构

**实现**:
```python
class MemoryOperator:
    """记忆操作决策引擎"""
    
    OPERATIONS = ['ADD', 'UPDATE', 'DELETE', 'NOOP']
    
    def decide_operation(self, new_memory: Dict, existing_memories: List[Dict]) -> str:
        """
        决定对新记忆执行什么操作
        
        Args:
            new_memory: 新提取的记忆
            existing_memories: 相关的已有记忆
        
        Returns:
            操作类型: ADD/UPDATE/DELETE/NOOP
        """
        # 1. 检查是否为噪声（虚假记忆过滤）
        if self._is_noise(new_memory):
            return 'NOOP'
        
        # 2. 检查是否存在冲突
        conflicts = self._find_conflicts(new_memory, existing_memories)
        
        if conflicts:
            # 3. 基于时间戳和置信度决定
            if self._should_update(new_memory, conflicts):
                return 'UPDATE'
            elif self._should_delete(new_memory, conflicts):
                return 'DELETE'
            else:
                return 'NOOP'
        else:
            return 'ADD'
    
    def _is_noise(self, memory: Dict) -> bool:
        """虚假记忆过滤器"""
        noise_patterns = [
            '数学计算', '琐碎 QA', '天气查询', 
            '时间查询', '单位换算', '临时指令'
        ]
        # 使用 LLM 或规则判断
        return any(pattern in memory['content'] for pattern in noise_patterns)
    
    def _find_conflicts(self, new: Dict, existing: List[Dict]) -> List[Dict]:
        """冲突检测"""
        conflicts = []
        for old in existing:
            # 检查实体重叠
            if set(new['entities']) & set(old['entities']):
                # 检查语义冲突
                if self._is_semantic_conflict(new['content'], old['content']):
                    conflicts.append(old)
        return conflicts
    
    def _should_update(self, new: Dict, conflicts: List[Dict]) -> bool:
        """基于时间戳和置信度决定是否更新"""
        for old in conflicts:
            # 新记忆更新 + 置信度更高 → UPDATE
            if new['timestamp'] > old['timestamp'] and new['confidence'] >= old['confidence']:
                return True
        return False
```

**工作量**: 15-20 小时

#### 1.2 增强 Memory Point 结构

**当前结构**:
```json
{
  "id": "f_001",
  "content": "用户对花生过敏",
  "type": "fact",
  "importance": 1.0,
  "score": 1.0,
  "entities": ["用户", "花生"]
}
```

**新结构**:
```json
{
  "id": "f_001",
  "content": "用户对花生过敏",
  "type": "fact",
  "importance": 1.0,
  "score": 1.0,
  "confidence": 1.0,
  
  // 新增字段
  "timestamp": "2026-02-14T10:30:00Z",
  "session_id": "session_123",
  "source_quote": "我对花生过敏，吃了会死",
  "source_turn": 15,
  "ownership": "user",  // user/assistant/third_party
  
  "entities": ["用户", "花生"],
  
  // 冲突管理
  "supersedes": ["f_000"],  // 取代的旧记忆 ID
  "superseded_by": null,
  "conflict_resolved_at": null
}
```

**工作量**: 5-8 小时

#### 1.3 显式冲突消解协议

**实现**:
```python
class ConflictResolver:
    """冲突消解器"""
    
    def resolve(self, new: Dict, old: Dict) -> Dict:
        """
        解决两条记忆的冲突
        
        策略:
        1. 时间优先: 新记忆 > 旧记忆
        2. 置信度优先: 高置信度 > 低置信度
        3. 来源优先: 用户陈述 > 推断
        
        Returns:
            解决方案: {'action': 'UPDATE', 'winner': new, 'loser': old}
        """
        # 时间戳比较
        if new['timestamp'] > old['timestamp']:
            time_score = 1
        else:
            time_score = -1
        
        # 置信度比较
        conf_score = new['confidence'] - old['confidence']
        
        # 来源优先级
        source_priority = {'user': 3, 'assistant': 2, 'third_party': 1}
        source_score = source_priority[new['ownership']] - source_priority[old['ownership']]
        
        # 综合评分
        total_score = time_score * 0.5 + conf_score * 0.3 + source_score * 0.2
        
        if total_score > 0:
            return {
                'action': 'UPDATE',
                'winner': new,
                'loser': old,
                'reason': f'时间={time_score}, 置信度={conf_score:.2f}, 来源={source_score}'
            }
        else:
            return {
                'action': 'KEEP',
                'winner': old,
                'loser': new,
                'reason': '旧记忆更可靠'
            }
```

**工作量**: 10-15 小时

#### 1.4 虚假记忆过滤器（FMR）

**目标**: FMR (False Memory Resistance) > 85%

**实现**:
```python
class NoiseFilter:
    """虚假记忆过滤器"""
    
    # 噪声模式（规则）
    NOISE_PATTERNS = [
        r'^\d+[\+\-\*/]\d+',  # 数学计算
        r'^what is \d+',      # 琐碎问答
        r'^今天天气',          # 天气查询
        r'^现在几点',          # 时间查询
        r'^帮我搜索',          # 临时指令
    ]
    
    # 噪声关键词
    NOISE_KEYWORDS = [
        '单位换算', '翻译', '定时器', '闹钟',
        '计算器', '搜索', '查询', '帮我找'
    ]
    
    def is_noise(self, content: str, context: Dict) -> bool:
        """
        判断是否为噪声
        
        Args:
            content: 记忆内容
            context: 上下文（对话轮次、类型等）
        
        Returns:
            是否为噪声
        """
        # 规则 1: 正则匹配
        for pattern in self.NOISE_PATTERNS:
            if re.match(pattern, content):
                return True
        
        # 规则 2: 关键词匹配
        if any(kw in content for kw in self.NOISE_KEYWORDS):
            return True
        
        # 规则 3: 长度过滤（太短的通常是噪声）
        if len(content) < 10:
            return True
        
        # 规则 4: 重要性过滤
        if context.get('importance', 0) < 0.3:
            return True
        
        return False
    
    def filter_batch(self, memories: List[Dict]) -> List[Dict]:
        """批量过滤"""
        return [m for m in memories if not self.is_noise(m['content'], m)]
```

**工作量**: 8-10 小时

---

### Phase 2: 性能与时序优化（重要）
**目标**: 对齐 LongMemEval 的百万级 Token 要求

**预计工作量**: 200 小时 → **简化为 60-80 小时**

#### 2.1 时序增强查询

**实现**:
```python
class TemporalQueryEngine:
    """时序查询引擎"""
    
    def rewrite_temporal_query(self, query: str, current_time: datetime) -> Dict:
        """
        重写时序查询
        
        Examples:
            "他上次说的话" → {"time_range": ["2026-02-13", "2026-02-14"]}
            "三个月前" → {"time_range": ["2025-11-14", "2025-11-14"]}
        """
        temporal_patterns = {
            r'上次': lambda: current_time - timedelta(days=1),
            r'昨天': lambda: current_time - timedelta(days=1),
            r'上周': lambda: current_time - timedelta(weeks=1),
            r'上个月': lambda: current_time - timedelta(days=30),
            r'(\d+)个月前': lambda m: current_time - timedelta(days=30*int(m.group(1))),
        }
        
        for pattern, time_func in temporal_patterns.items():
            match = re.search(pattern, query)
            if match:
                target_time = time_func() if callable(time_func) else time_func(match)
                return {
                    'time_range': [
                        target_time.isoformat(),
                        (target_time + timedelta(days=1)).isoformat()
                    ]
                }
        
        return {}
    
    def apply_time_decay(self, memories: List[Dict], current_time: datetime) -> List[Dict]:
        """应用时间衰减"""
        for m in memories:
            created = datetime.fromisoformat(m['created'])
            days_elapsed = (current_time - created).days
            
            # 衰减函数: γ = e^(-λΔt)
            lambda_decay = 0.01  # 衰减率
            decay_factor = math.exp(-lambda_decay * days_elapsed)
            
            m['time_decayed_score'] = m['score'] * decay_factor
        
        return memories
```

**工作量**: 15-20 小时

#### 2.2 事实演变追踪

**实现**:
```python
class FactEvolutionTracker:
    """事实演变追踪器"""
    
    def track_evolution(self, entity: str, attribute: str) -> List[Dict]:
        """
        追踪实体属性的演变历史
        
        Example:
            entity="用户", attribute="location"
            → [
                {"value": "北京", "valid_from": "2025-01-01", "valid_to": "2026-01-01"},
                {"value": "上海", "valid_from": "2026-01-01", "valid_to": None}
            ]
        """
        # 查询所有相关记忆
        memories = self.backend.search_by_entities([entity])
        
        # 提取属性变化
        evolution = []
        for m in sorted(memories, key=lambda x: x['timestamp']):
            if attribute in m['content']:
                value = self._extract_value(m['content'], attribute)
                evolution.append({
                    'value': value,
                    'valid_from': m['timestamp'],
                    'valid_to': None,
                    'memory_id': m['id']
                })
                
                # 更新上一条的 valid_to
                if len(evolution) > 1:
                    evolution[-2]['valid_to'] = m['timestamp']
        
        return evolution
    
    def get_current_value(self, entity: str, attribute: str, at_time: datetime = None) -> str:
        """获取指定时间点的属性值"""
        evolution = self.track_evolution(entity, attribute)
        
        at_time = at_time or datetime.now()
        
        for item in reversed(evolution):
            valid_from = datetime.fromisoformat(item['valid_from'])
            valid_to = datetime.fromisoformat(item['valid_to']) if item['valid_to'] else datetime.now()
            
            if valid_from <= at_time <= valid_to:
                return item['value']
        
        return None
```

**工作量**: 20-25 小时

#### 2.3 证据追踪（Evidence Tracking）

**实现**:
```python
class EvidenceTracker:
    """证据追踪器"""
    
    def add_evidence(self, memory: Dict, source_turn: int, source_quote: str):
        """添加证据"""
        memory['evidence'] = {
            'turn': source_turn,
            'quote': source_quote,
            'session_id': memory['session_id'],
            'timestamp': memory['timestamp']
        }
    
    def get_evidence_chain(self, memory_id: str) -> List[Dict]:
        """获取证据链"""
        memory = self.backend.get_memory(memory_id)
        
        chain = [memory['evidence']]
        
        # 如果是更新，追溯原始证据
        if memory.get('supersedes'):
            for old_id in memory['supersedes']:
                old_memory = self.backend.get_memory(old_id)
                if old_memory and old_memory.get('evidence'):
                    chain.append(old_memory['evidence'])
        
        return chain
    
    def format_for_locomo(self, memory: Dict) -> Dict:
        """格式化为 LoCoMo 要求的输出"""
        return {
            'answer': memory['content'],
            'evidence_ids': [e['turn'] for e in self.get_evidence_chain(memory['id'])],
            'confidence': memory['confidence']
        }
```

**工作量**: 10-15 小时

---

### Phase 3: 高阶智能演进（可选）
**目标**: PersonaMem v2 突破

**预计工作量**: 350 小时 → **暂缓，等 Phase 1-2 完成后评估**

---

## 📋 立即执行计划（v1.3.0）

### 优先级排序

| 任务 | Phase | 工作量 | 优先级 | 依赖 |
|------|-------|--------|--------|------|
| Memory Operator | 1.1 | 15-20h | 🔴 P0 | - |
| 增强 MP 结构 | 1.2 | 5-8h | 🔴 P0 | - |
| 冲突消解协议 | 1.3 | 10-15h | 🔴 P0 | 1.1, 1.2 |
| 虚假记忆过滤 | 1.4 | 8-10h | 🔴 P0 | 1.1 |
| 时序查询引擎 | 2.1 | 15-20h | 🟡 P1 | 1.2 |
| 事实演变追踪 | 2.2 | 20-25h | 🟡 P1 | 1.2, 1.3 |
| 证据追踪 | 2.3 | 10-15h | 🟡 P1 | 1.2 |

### 总工作量估算

- **Phase 1 (P0)**: 38-53 小时
- **Phase 2 (P1)**: 45-60 小时
- **总计**: 83-113 小时

---

## 🎯 成功指标

### Phase 1 完成标准

| 指标 | 目标 | 验证方法 |
|------|------|---------|
| 虚假记忆过滤率 (FMR) | > 85% | HaluMem 测试集 |
| 冲突消解准确率 | > 90% | 手动构造冲突场景 |
| 操作决策准确率 | > 80% | ADD/UPDATE/DELETE 决策测试 |

### Phase 2 完成标准

| 指标 | 目标 | 验证方法 |
|------|------|---------|
| 时序查询准确率 | > 75% | LongMemEval 时序子集 |
| 事实演变追踪准确率 | > 80% | 手动构造演变场景 |
| 证据溯源完整性 | 100% | LoCoMo 证据返回测试 |

---

## 🚀 下一步行动

1. **立即开始 Phase 1.1**: Memory Operator 实现
2. **并行进行 Phase 1.2**: 增强 MP 结构（修改 Schema）
3. **完成 Phase 1 后**: 运行 HaluMem 测试，验证效果
4. **根据测试结果**: 决定是否进入 Phase 2

---

**文档版本**: v1.0  
**创建时间**: 2026-02-14 04:50 UTC  
**作者**: Tkao  
**基于**: 评测集深度调研报告
